// TEMPORARY — UI testing only. Set to false (or delete this file) before any release.
// When true, auth guards are skipped so every screen can be reached without a session.
export const DEV_BYPASS_AUTH = false;
